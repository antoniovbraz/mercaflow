# =============================================================================
# MERCA FLOW - CONFIGURAÇÕES DE AMBIENTE
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE
# Obtenha suas chaves em: https://app.supabase.com/project/_/settings/api
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# MERCADO LIVRE API
# Registre sua aplicação em: https://developers.mercadolibre.com.br/
# Configure callback URL: {NEXT_PUBLIC_SITE_URL}/api/ml/auth/callback
# -----------------------------------------------------------------------------
ML_CLIENT_ID=6829614190686807
ML_CLIENT_SECRET=your-ml-client-secret
ENCRYPTION_KEY=your-32-byte-hex-encryption-key

# -----------------------------------------------------------------------------
# OPENAI (OPCIONAL)
# Para funcionalidades de IA - Otimização de preços e títulos
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# REDIS (RECOMENDADO)
# Para cache e performance - Upstash Redis (integra via Vercel)
# Setup: Vercel Dashboard > Integrations > Upstash Redis
# Reduz chamadas à API ML em 70-80%, dashboard < 1s
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-rest-token
REDIS_URL=rediss://default:your-token@your-instance.upstash.io:6379

# -----------------------------------------------------------------------------
# MONITORAMENTO (OPCIONAL)
# Para tracking de erros e performance
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn

# -----------------------------------------------------------------------------
# APLICAÇÃO
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_SHARP=0
NODE_ENV=development

# -----------------------------------------------------------------------------
# SUPER ADMINS
# Lista de emails que terão acesso de super admin (separados por vírgula)
# -----------------------------------------------------------------------------
SUPER_ADMIN_EMAILS=seu-email@exemplo.com,outro-admin@exemplo.com

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO ML - INSTRUÇÕES
# -----------------------------------------------------------------------------
# 
# 1. CRIAR APLICAÇÃO ML:
#    - Acesse https://developers.mercadolivre.com.br/console/
#    - Crie nova aplicação com nome "MercaFlow"
#    - Configure callback: {NEXT_PUBLIC_SITE_URL}/ml/callback
#    - Copie Client ID (já preenchido) e Secret Key
#
# 2. GERAR CHAVE DE CRIPTOGRAFIA:
#    Execute: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    Cole o resultado em ENCRYPTION_KEY
#
# 3. EXECUTAR MIGRAÇÕES:
#    supabase db push
#    Aplica o schema da integração ML ao banco
#
